import React from "react";

import "./contacts.css";

const Contacts = () => {
  return (
    <div className="contacts" id="contacts">
      This is Contacts
    </div>
  );
};

export default Contacts;
